# configs/gcn_ssm.yaml
model:
  name: "CONV"
  params:
    in_ch: 1
    out_ch: 1
    num_layers: 15
    n_channels: 8
    kernel_size: 12

training: 
  optimizer: 
    type: "differential"
    learning_rate: 2.0e-3
    core_lr_factor: 0.1
    other_weight_decay: 0.04